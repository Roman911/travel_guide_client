export type Locations = {
  _id: string
  coordinates: string[]
  isType: string
}[]

export type Location = {
  address: string[]
  cover: {
    url: string
  }
  small_text: string
  title: string
  _id: string
  post: {
    _id
  }
}